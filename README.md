This is an AI-powered agent that automates sending personalized birthday emails to team members.
It stores names, emails, and birthdates, and checks daily for matching birthdays.
When a match is found, it uses an LLM (OpenAI API) to generate a unique birthday message.
The generated message is then sent via Gmail SMTP to the recipient.
